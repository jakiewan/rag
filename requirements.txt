# 整合脚本依赖
# 安装: pip install -r requirements.txt

python-dotenv>=1.0.0
minio>=7.2.0
openai>=1.0.0
cryptography>=41.0.0

# MinerU 解析
magic-pdf>=1.0.0

# 02 chunking + embedding（notebook 实际依赖）
langchain-text-splitters>=0.3.0
pymilvus>=2.4.0
FlagEmbedding>=1.2.0

# FlagEmbedding 间接依赖，但显式声明便于换环境复现
torch>=2.0.0
transformers>=4.40.0